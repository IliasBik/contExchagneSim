# Формула прибыли и её оптимизация: mean-field приближение

Документ к `mean_field.py`. Продолжение `optimization.md`: там задача записана
как чёрный ящик, здесь чёрный ящик раскрывается — клиринг CE решается явно,
лимитные биржи заменяются экзогенным шумом, и ожидаемая прибыль
записывается формулой, которую можно считать за миллисекунды и оптимизировать
аналитически. Все константы взяты из калибровки рынка без агентов
(`mean_field_calib.json`), все утверждения проверены редуцированной и полной
симуляцией (раздел 7).

---

## 0. Итог в трёх фразах

1. При четырёх заявках клиринг CE — это **контур**: за тик по кольцу
   $T1 \to AY \to T2 \to AX \to T1$ проходит один и тот же нотионал
   $V_t = \varepsilon_t / R$, где $\varepsilon_t$ — суммарное расхождение котировок
   по кольцу, а $R = \sum_a 1/\lambda_a$ — «сопротивление». Каждый агент
   получает долю $\varphi_a = (1/\lambda_a)/R$ этого расхождения на каждой единице оборота.
2. Внутри CE стоимость не создаётся; вся прибыль экосистемы — это то, что
   трансляторы выручают на лимитных биржах сверх справедливой цены:
   $$\mathbb{E}[\Phi] \approx T \sum_{v=1,2} \mathbb{E}\big[\tilde h_v \cdot \pi_v\big],$$
   объём хеджа $\tilde h_v$, умноженный на **исполняемую премию** $\pi_v$ (насколько
   лучший бид выше якоря при продаже, насколько лучший аск ниже якоря при покупке).
3. Оптимум по $\theta$ — компромисс между потоком ($1/R$, растёт при малых $h_m$)
   и риском склада ($T1$ и арбитражёры держат инвентарь; его переоценка даёт
   взрывы). При этом найденные GP «оптимумы» лежат в вырожденном режиме, где
   шейдинг арбитражёров упирается в клэмп и суммарный PnL наполовину состоит
   из артефакта марок (раздел 8).

---

## 1. Точное решение клиринга: контур

Обозначения из `optimization.md`. За тик активны ровно четыре заявки:

| агент | портфель $w$ | $\ln z$ | $\lambda$ (в X1) | знак $s_a$ |
|---|---|---|---|---|
| T1 | $e_{Y1}-e_{X1}$ | $\ln m_1 - g_1 u_1$ | $\kappa_T H_1/h_{m1}$ | $+$ |
| T2 | $e_{Y2}-e_{X2}$ | $\ln m_2 - g_2 u_2$ | $\kappa_T H_2 P_{X2}/h_{m2}$ | $-$ |
| AX | $e_{X1}-e_{X2}$ | $-g_X u_X$ | $\kappa_A C_X/h_{mx}$ | $+$ |
| AY | $e_{Y1}-e_{Y2}$ | $-g_Y u_Y$ | $\kappa_A C_Y/h_{my}$ | $-$ |

($u_a$ — стоимость длинной ноги агента; $g_a$ — риск-коэффициенты из 4.3–4.4
`optimization.md`.) Четыре портфеля образуют кольцо на трёх свободных
лог-ценах ($S_{X1}=0$), поэтому невязки $r_a = w_a\cdot S - \ln z_a$ связаны одним
тождеством $\sum_a s_a r_a = -\varepsilon$, где

$$
\varepsilon_t \;=\; \ln z_{T1} - \ln z_{T2} + \ln z_{AX} - \ln z_{AY}
\;=\; \underbrace{\ln\frac{m_1}{m_2}}_{\delta_t}
\;-\; g_1 n_1 - g_2 n_2 - g_X n_A - g_Y n_A ,
$$

$n_a$ — инвентари, отсчитанные в направлении положительного потока
($n_1 = u_1$, $n_2 = -u_2$, $n_A = u_X = -u_Y$). Минимизация
$\sum_a \lambda_a r_a^2$ при этом ограничении даёт (метод Лагранжа)

$$
r_a = -\frac{s_a\,\varepsilon}{\lambda_a R},\qquad
V_a = \lambda_a f_a = s_a\,\frac{\varepsilon}{R},\qquad
R = \sum_a \frac{1}{\lambda_a},\qquad
\boxed{\,V_t = \frac{\varepsilon_t}{R}\,}
$$

— **через каждую заявку проходит один и тот же нотионал** $V_t$ (ток в
последовательной цепи), и все четыре инвентаря меняются на $+V_t$. Цены:
$S_{Y1} = \ln z_{T1} - \varphi_1\varepsilon$, $S_{X2} = -\ln z_{AX} + \varphi_X\varepsilon$,
$S_{Y2} = S_{X2} + \ln z_{T2} + \varphi_2\varepsilon$, где
$\varphi_a = (1/\lambda_a)/R$, $\sum_a\varphi_a = 1$: каждый агент исполняется по
своей котировке, сдвинутой на **свою долю** расхождения $\varphi_a\varepsilon$ в свою
пользу. Проверено на симуляции: филлы совпадают с $\pm\varepsilon/R$ с
относительной ошибкой $10^{-10}$.

Следствие для параметров: $\theta$ входит только через
$\Lambda_a = 1/\lambda_a$ (сумма $R$ и доли $\varphi_a$) и $\Gamma_v = g_v$, как и
предполагалось в `optimization.md`, раздел 6.

---

## 2. Точное разложение суммарного PnL

Клиринг самофинансируем: $\sum_j \Delta Q^{CE}_{a,j} P_j = V_a \sum_j w_{a,j} = 0$ и
$\sum_a V_a w_a = 0$. Поэтому совокупная позиция экосистемы состоит только из
сделок на биржах, и

$$
\Phi = \sum_i E_i(T)
= \underbrace{\sum_t \tilde h_1(t)\Big(\frac{\bar p_1(t)}{P_{Y1}(T)}-1\Big)P_{Y1}(T)\cdots}_{\text{биржа 1}}
+ \underbrace{\cdots}_{\text{биржа 2}}
\;=\; \sum_{v}\ \sum_{t}\ \tilde h_v(t)\,\big[\text{цена исполнения на бирже } v\big] \;-\; \big[\text{марка CE той же позиции в } T\big].
$$

В развёрнутом виде ($h_v$ — штуки, $\bar p_v$ — средняя цена исполнения,
$H_v = \sum_t h_v$):

$$
\Phi = \sum_t h_1(t)\bar p_1(t) - H_1 P_{Y1}(T)
\;+\; H_2 P_{Y2}(T) - P_{X2}(T)\sum_t h_2(t)\bar p_2(t).
$$

Это точное тождество; всё остальное — перераспределение между четырьмя
агентами. Оно же показывает, где сидит опасность: марки $P_{Y1}, P_{Y2}, P_{X2}$
задают сами агенты, и если $P_{X2}$ далёк от 1 (раздел 8), $\Phi$ измеряет не
торговый результат.

---

## 3. Матожидание: формула прибыли

### 3.1 Регулярный режим и опорная цена

Предположения (все проверены в разделе 7): инвентари арбитражёров малы, так
что $P_{X2}\approx 1$, $P_{Y1}\approx m_1$, $P_{Y2}\approx m_2$; уровень цен
$a_t$ (якорь) — мартингал относительно информации агентов; шум мидов
$\eta_v = \ln(m_v/a)$ стационарен. Запишем лучший бид и аск биржи $v$ как
$a\,e^{\beta_v}$ и $a\,e^{\alpha_v}$; тогда $\eta_v = (\alpha_v+\beta_v)/2$,
полуспред $c_v = (\alpha_v-\beta_v)/2$.

Подставляя в тождество раздела 2 и беря матожидание (мартингальные члены
$h(t)\,(a_t - a_T)$ выпадают, концевые эффекты $O(1)$):

$$
\boxed{\;
\mathbb{E}[\Phi] \;\approx\; T\sum_{v=1,2}\mathbb{E}\big[\tilde h_v\,\pi_v\big]
\;-\;T\sum_v \frac{k_v}{2m}\,\mathbb{E}\big[\tilde h_v^2\big],
\qquad
\pi_v = \begin{cases}\beta_v, & \text{продажа на }v\\ -\alpha_v, & \text{покупка на }v\end{cases}
\;}
$$

$\tilde h_v$ — стоимость, хеджированная за тик (со знаком), $\pi_v$ —
**исполняемая премия**: продать на бирже выше якоря, купить ниже якоря;
$k_v$ — наклон книги (предельный импакт). Эквивалентная запись через мид:
$\pi_v = \pm\eta_v - c_v$, то есть «шум мида минус полуспред» — но
существенно, что $\eta_v$ и $c_v$ **коррелированы** во времени (мид низок
чаще всего потому, что далеко ушёл бид, а не потому, что аск дёшев), и
транслятор хеджируется только при $c_v(t) < g_v |u_v|$. Формула с
$\mathbb{E}\eta$ и $\mathbb{E}c$ по отдельности занижает прибыль в разы (раздел 7).

### 3.2 Кто и когда хеджируется: поток через контур

Инвентарь транслятора $v$ после клиринга $n'_v = n''_v + V_t$; правило
`hedge_plan` для линейной книги даёт хедж

$$
\tilde h_v = \phi_v\,\big(|n'_v| - n^*_v(t)\big)_+\,\operatorname{sgn} n'_v,\qquad
n^*_v(t) = \frac{c_v(t)}{g_v},\qquad
\phi_v = \frac{g_v m}{g_v m + k_v},
$$

— мёртвая зона шириной «полуспред / риск-коэффициент», за ней почти полный
сброс излишка ($\phi_v \approx 1$). Пока транслятор сидит на границе мёртвой
зоны, его шейдинг равен $g_v n^*_v = c_v$: **он котирует на CE свой бид/аск, а
не мид**, и полуспред перекладывается на контрагентов по кольцу. Немедленный
хеджер (T2 при $h_{r2}\gtrsim 1$) поэтому зарабатывает ровно свою долю
расхождения: $\mathbb{E}[\text{PnL}_{T2}] \approx (\varphi_2/R)\sum_t \varepsilon_t^2$
(проверено: реальная и редуцированная симуляции дают T2 в плюсе).
Склад (T1 при $h_{r1}\ll 1$: $n^*_1 \to\infty$, шейдинг $\to 0$) не хеджируется и
несёт переоценку $\sum_t n_1(t-1)\,\Delta\ln P_{Y1}(t)$.

Поток $V_t = \varepsilon_t/R$ с $\varepsilon_t = \delta_t - c_2\operatorname{sgn}(n_2) - G\,n_A$, где
$G = g_1 + g_X + g_Y$ — суммарный шейдинг «склада» (T1 + арбитражёры). Три
режима:

* **бесконечный склад** ($G\to 0$): $V_t = (\delta_t - c_2\operatorname{sgn}\delta_t)_+/R$ —
  поток идёт, пока $|\delta|$ превышает полуспред тонкой биржи; инвентарь
  $n_A = \sum V$ блуждает без возврата — риск взрыва;
* **конечный склад**: $n_A$ релаксирует со скоростью $G/R$ за тик;
  при $G/R > 2$ дискретный контур **перезаряжается за тик** и осциллирует
  (неустойчивость), при $G/R \in (1,2)$ — колеблется с затуханием;
* **жёсткий контур** ($1/R\to\infty$): $\varepsilon\to 0$ каждый тик, инвентарь
  склада $n_A = (\delta - c_2\operatorname{sgn})/G$ отслеживает $\delta$ мгновенно.

### 3.3 Нулевое приближение: знак и масштаб

Если T2 хеджируется немедленно, T1 — склад без шейдинга, арбитражёры —
проводники, а $\delta$ гауссов с независимыми компонентами
($\sigma_1 = 1.23\%$, $\sigma_2 = 2.20\%$, $\sigma_\delta = 2.6\%$):

$$
\mathbb{E}[\Phi] \approx \frac{T}{R}\,
\mathbb{E}\Big[(|\delta| - c_2)_+\,\big(r_2|\delta| - c_2\big)\Big],
\qquad r_2 = \frac{\sigma_2^2}{\sigma_\delta^2} = 0.76,
$$

где $r_2|\delta|$ — регрессия $-\eta_2$ на $\delta$ (доля расхождения, которая
действительно «сидит» на бирже 2). Аналог для хеджа T1 имеет $r_1 = 0.24$, и
$r_1|\delta| - c_1 < 0$ при всех реалистичных $|\delta|$: **хеджировать на толстой
бирже по этой формуле невыгодно, на тонкой — выгодно с тонкой маржой**
($\sigma_2^2/\mathbb{E}|\delta| = 2.4\%$ против $c_2 = 1.8\%$). Это объясняет структуру
всех хороших точек GP: $h_{r1}\to 0$, $h_{r2}\gtrsim 1$, $h_{m}\to$ малые.
Прибыль **линейна по потоку** $1/R$, поэтому в нулевом приближении
оптимизатор упирается в границы по $h_m$; интерьерный оптимум задаётся
только риском склада и импактом.

---

## 4. Замкнутые формулы: линейно-гауссовская модель (`lg_eval`)

Состояние $x_t = (n''_1, n''_2, n''_A, \eta_1, \eta_2)$ — инвентари после хеджей и
шум мидов; $\eta_{t+1} = \Phi\eta_t + \xi_{t+1}$ (VAR(1), калибровка:
$\operatorname{diag}\Phi = (0.67, 0.82)$, компоненты почти независимы). За тик:

$$
n' = \Big(I - \tfrac{1}{R}\mathbf 1 g^\top\Big) n'' + \tfrac{1}{R}\mathbf 1\, d^\top\eta,\quad
d = (1,-1);\qquad
n''_v = (1-\beta_v)\,n'_v,\quad n''_A = n'_A,
$$

$$
x_t = A x_{t-1} + G\xi_t,\qquad
\Sigma_t = A\Sigma_{t-1}A^\top + GQG^\top,\quad \Sigma_0 = \operatorname{diag}(0,0,0,\Sigma_\eta).
$$

Ковариация берётся на конечном горизонте $T$ (старт из нулевых инвентарей;
сумма $\sum_{t\le T}\Sigma_t$ считается удвоением за $O(\log T)$), потому что при
$h_{r1}\to 0$ у системы есть маргинально устойчивое направление и
стационарной ковариации нет. Мёртвая зона хеджа линеаризуется статистически
($n'\sim N(0,\sigma^2)$, $\kappa = n^*/\sigma$):

$$
\beta_v = 2\phi_v\,\bar\Phi(\kappa),\qquad
\mathbb{E}|\tilde h_v| = 2\phi_v\sigma\,[\varphi(\kappa) - \kappa\bar\Phi(\kappa)],\qquad
\mathbb{E}\tilde h_v^2 = 2\phi_v^2\sigma^2\,[(1+\kappa^2)\bar\Phi(\kappa) - \kappa\varphi(\kappa)],
$$

и $\mathbb{E}[\tilde h_v\eta_v] = \beta_v\operatorname{Cov}(n'_v,\eta_v)$ (точно для гауссовских
пар). Итого

$$
\pi(\theta) = \beta_1\operatorname{Cov}(n'_1,\eta_1) - \beta_2\operatorname{Cov}(n'_2,\eta_2)
- \sum_v\Big(c_v\,\mathbb{E}|\tilde h_v| + \frac{k_v}{2m}\mathbb{E}\tilde h_v^2\Big),\qquad
\mathbb{E}[\Phi] \approx T\pi(\theta).
$$

Риск-коэффициенты арбитражёров $g_X, g_Y$ самосогласованы: дисперсия
приращений базиса $\ln(P_{X1}/P_{X2}) = -g_X n''_A - \varphi_X\varepsilon$ — линейный
выход той же системы. Вероятность взрыва: PnL агента $i$ за тик меняется на
(экспозиция) $\times$ (приращение его марки), обе величины — линейные выходы;
$P(\max_t|E_i| > B) \approx 4\bar\Phi\big(B/\sqrt{T\,\mathbb{E}[e_i^2]\operatorname{Var}\Delta y_i}\big)$.
Задача:

$$
\max_{\theta}\ T\pi(\theta)\quad\text{при}\quad p_{\text{blow}}(\theta)\le p_{\max},
$$

шесть переменных, гладкая, одно вычисление $\approx 50$ мс, оптимизация —
минуты вместо часов (`lg_optimize`).

**Ограничение LG-модели.** В ней спред постоянен ($c_v = \mathbb{E}c_v$), а на
деле транслятор хеджируется в моменты узкого спреда и его эффективная цена —
нижний хвост распределения $c_v(t)$; кроме того, «дешёвый мид» с широким
спредом — не исполняемая возможность. Поэтому LG правильно передаёт структуру
(кто хеджирует, роль $1/R$, риск склада), но занижает уровень прибыли и
ошибается в знаке для хеджа T1 (раздел 7). Уточнение — усреднить моменты
мёртвой зоны по распределению $c_v$ и считать стоимость как
$\mathbb{E}[c_v\mid c_v < g_v|u_v|]$; полностью это учитывает только
редуцированная симуляция на записанных путях.

---

## 5. Редуцированная симуляция как «вычислимое матожидание» (`reduced_run`)

Те же уравнения агентов, что в `opt_simulation`, но клиринг — формулой
раздела 1, а рынок — записанный путь без агентов (миды, якорь, спреды,
глубина, волатильность на каждом тике) плюс линейная книга за лучшим
уровнем ($k_v$). Один прогон 6000 тиков — 0.4 с против 60–80 с у полной
симуляции; шесть путей с общими случайными числами дают гладкую по $\theta$
целевую функцию. Что при этом отброшено: влияние хеджей на стакан (измерено
импульсным откликом: разовый хедж до 0.5 шт мид не сдвигает, 4 шт на тонкой
бирже сдвигают на 1.7% с восстановлением за 5 тиков — при потоках порядка
0.01 шт/тик пренебрежимо), реальная гранулярность уровней книги, срезание
$|g u|\le 1$ сохранено.

---

## 6. Калибровка (рынок без агентов, 6 сидов $\times$ 6000 тиков)

| величина | биржа 1 | биржа 2 |
|---|---|---|
| полуспред $c_v$ (средняя потеря при малом хедже) | 0.94% | 1.79% |
| предельный импакт $k_v$, на штуку | 0.38% | 0.75% |
| $H_v$ | 98.9 | 12.4 |
| $\sigma_v^2$ (EWMA приращений мида за тик) | $9.6\cdot10^{-5}$ | $1.7\cdot10^{-4}$ |
| std $\eta_v = \ln(m_v/a)$ | 1.23% | 2.20% |
| AR(1) $\eta_v$ | 0.67 | 0.82 |

$\delta = \eta_1-\eta_2$: std 2.6%, $\mathbb{E}|\delta| = 2.08\%$, AR(1) 0.81;
$\operatorname{corr}(\eta_1,\eta_2) = -0.07$; std приращения $\ln a$ за тик 0.107%;
$\gamma = 2.66\cdot10^4$; тиков без котировки T2 (пустая сторона книги) 4.2%.
Маржа «за единицу потока»: $\mathbb{E}[-\delta\eta_2]/\mathbb{E}|\delta| = 2.41\%$ (биржа 2),
$\mathbb{E}[\delta\eta_1]/\mathbb{E}|\delta| = 0.83\%$ (биржа 1).

---

## 7. Проверка: полная симуляция против редуцированной и LG

Сиды 101–106, $T = 6000$, медиана / среднее суммарного PnL, число взрывов.

| точка | LG $T\pi$ | reduced, записанные пути | reduced, гауссовские пути | полная симуляция |
|---|---|---|---|---|
| baseline (все 1) | 0.0 | +2.4 / +2.5, 0 | 0.0 / 0.0, 0 | +2.5 / +2.4, 0 |
| opt_best | +19.9 | +74.1 / +66.6, 0 | +2.8 / +3.4, 0 | +24.9 / −126.7, 1 |
| opt_best, $h_{r1}/100$ | +22.6 | +67.0 / +59.2, 0 | +6.1 / +6.6, 0 | +49.7 / +48.7, 0 |
| opt_best, $h_{r1}=4$ (T1 хеджирует) | −56.0 | +74.4 / +64.9, 0 | −210 / −210, 0 | +31.9 / −124.8, 1 |
| оптимум LG с ограничениями (раздел 9) | +888 | −1000 / −1000, 6 | −1000 / −1000, 20 | — |
| оптимум reduced по медиане 6 путей (раздел 9) | −113 | +160 / −42, 1 | — | −475 / −438, 3 |
| оптимум reduced по среднему, справедливые марки (раздел 9) | +35.3 | +82.6 / +81.8, 0 | — | +77.0 / +67.4, 0 |

Выводы:

* в baseline редуцированная модель воспроизводит полную симуляцию **по
  каждому агенту** (T1 −0.6/−0.7, T2 −1.3/−1.6, AX +2.2/+2.3, AY +2.3/+2.4);
* на сиде 102 (opt_best) полная симуляция остаётся в регулярном режиме и даёт
  T1 +24, T2 +0.5, AX +12, AY +39, всего +76 — редуцированная на своих путях:
  T1 +26, T2 +6, AX +11, AY +32, всего +76;
* гауссовские пути с постоянным спредом занижают прибыль на порядок и меняют
  знак для хеджирующего T1: эффект «хеджируюсь, когда спред узкий» — первого
  порядка, его нельзя отбрасывать;
* LG даёт правильный порядок в регулярном режиме, но наследует ошибку
  постоянного спреда;
* расхождение полной симуляции с редуцированной на сидах 101, 103 — не ошибка
  модели, а смена режима (раздел 8).

---

## 8. Вырожденный режим: срыв шейдинга арбитражёров и артефакт марок

Риск-коэффициент арбитражёра $g_X = \gamma\sigma_X^2/C$ берёт $\sigma_X^2$ из
EWMA приращений **базиса на CE**, а базис $\ln(P_{X1}/P_{X2}) = -g_X n_A - \varphi_X\varepsilon$
двигают его же котировки: $\sigma_X^2 \approx g_X^2\operatorname{Var}V + \varphi_X^2\operatorname{Var}\Delta\varepsilon$.
Уравнение на неподвижную точку $g_X = \gamma(g_X^2\operatorname{Var}V + \dots)/C$ квадратично: у
него есть малый устойчивый корень и большой неустойчивый; при большом потоке
($\operatorname{Var}V$ велика, то есть $h_{mx}, h_{my}$ малы) всплеск переводит систему за
неустойчивый корень, $g_X$ растёт лавинообразно и шейдинг упирается в клэмп
$|g u|\le 1$. Трассировка сида 101 в точке opt_best: до тика 500 $S_{X2}\approx 0$, в
окне 500–1000 $|\ln z_{AX}|, |\ln z_{AY}|$ уходят к 0.99, $S_{X2}\to -1.0$
($P_{X2} = 0.37$), разрыв $S_{Y1}-S_{Y2}$ в этом окне имеет std 41%, и дальше
система до конца прогона сидит в клэмпе с инвентарём арбитражёров
200–300 X1. За эти 500 тиков PnL T1 меняется с +2.9 до +465, AY с +2.5 до −398.

Последствия для целевой функции:

| сид, точка | режим | $P_{X2}(T)$ | $\Phi$ по маркам CE | $\Phi$ при $X2=1$, $Y=$ мид |
|---|---|---|---|---|
| 102, opt_best | регулярный | 1.000 | +76.1 | +76.0 |
| 101, opt_best | клэмп | 0.368 | +58.9 | +174.5 |
| 101, opt_best/100 | клэмп | 0.368 | +63.6 | +181.5 |
| 103, opt_best/100 | клэмп | 0.369 | +23.3 | +79.7 |

В клэмпе экосистема набирает крупную **направленную** позицию на бирже 2
(сид 101: продано 4.8 Y2 за 644 X2), и результат определяется тем, куда
ушёл уровень цен, — это та самая «лотерея $\pm 10^3$», и медиана GP собрана из
смеси регулярных (+76) и вырожденных (+23…+64 или −1000) сидов.

**Аналитический индикатор срыва.** Пусть $a = \gamma\operatorname{Var}(V)/c_0$,
$b = \gamma\varphi_A^2\operatorname{Var}(\Delta\varepsilon)/c_0$ (обе дисперсии — линейные
выходы LG-модели). Неподвижная точка $g_A = a g_A^2 + b$ существует только при
$4ab \le 1$; при $4ab\to 1$ устойчивый корень сливается с неустойчивым.
Значения $4ab$: baseline 0.001, opt_best 0.65 (срыв на 2 сидах из 6),
opt_best с $h_{r1}=4$ 6.2, «лотерейная» точка ниже 8.2. Второй показатель —
разряд контура за тик $G/R$ ($G = g_1 + g_2(1-\beta_2) + g_X + g_Y$): 0.03,
0.94, 2.0, 1.4 соответственно; при $G/R > 1$ контур перезаряжается за тик и
осциллирует. Регулярный режим — это $4ab \ll 1$ и $G/R < 1$, и оба условия
считаются в замкнутой форме через $\theta$.
Отсюда две рекомендации до любой дальнейшей оптимизации: (а) оценивать
суммарный PnL по справедливым маркам ($P_{X2}=1$, $P_{Y_v}=m_v$) или хотя бы
следить за $P_{X2}$; (б) сделать шейдинг арбитражёров устойчивым — брать
$\sigma^2$ для $g_X, g_Y$ из волатильности бирж, а не из собственного базиса,
либо ограничить $g_X n_A$ заметно ниже клэмпа. В регулярном режиме формулы
разделов 3–4 применимы, и оптимизация по ним осмысленна.

---

## 9. Что даёт оптимизация формулы — и почему задача в текущем виде не имеет интерьерного оптимума

Что было сделано и что вышло:

| оптимизатор | найденная точка | обещание модели | проверка |
|---|---|---|---|
| GP по полной симуляции (`opt_log.csv`) | $h_m\sim 10^{-2}$, $h_{r1}\to 0$, $h_{r2}\sim 4$ | медиана +25…+90 | смесь регулярных сидов (+76) и клэмпа (взрывы) |
| LG, $\max T\pi$ при $p_{\text{blow}}\le 0.25$, $4ab\le 0.5$, $G/R\le 1$ | $1/R = 1744$, T1 — склад | +888 | reduced: взрыв 6/6 |
| reduced, медиана по 6 путям | $1/R\approx 200$, $4ab = 8$ | +160 | полная симуляция: медиана −475, взрывы 3/6 |
| reduced, медиана по справедливым маркам с отсечением клэмпа | $1/R\approx 300$ | +165 | 2 из 6 путей всё равно вырождены |
| reduced, **среднее** по справедливым маркам (любой вырожденный путь = −1000) | $\theta^\star$ ниже, $1/R = 111$, $4ab = 0.42$, $G/R = 0.68$ | +83, 0 вырожденных | полная симуляция: медиана **+77**, среднее +67, std 32, **взрывов 0/6** |

Найденная регулярная точка:
$h_{m1} = 0.33,\ h_{r1} = 0.48,\ h_{m2} = 0.0079,\ h_{r2} = 1.0,\ h_{mx} = 0.030,\ h_{my} = 0.016$
(T1 хеджирует умеренно, T2 — агрессивный транслятор тонкой биржи с
нейтральным риском, арбитражёры — проводники, но не настолько, чтобы сорвать
шейдинг). По среднему это лучше всех точек `opt_log.csv` с нулём взрывов; по
медиане сопоставимо с лучшими устойчивыми (+50).

Первые четыре строки ведут себя одинаково: прибыль (раздел 3.3) **линейна по потоку
$1/R$**, ничего внутри регулярного режима её не ограничивает, и любой
оптимизатор упирается в границу, за которой начинаются взрывы и клэмп; медиана
как критерий ещё и терпит до половины «лотерейных» сидов. Значит, интерьерного
оптимума у задачи $\max\operatorname{med}\Phi$ в нынешней постановке нет — есть
только граница вырождения, положение которой задают $4ab$ и $G/R$ (раздел 8),
а не экономика хеджа. Последняя строка показывает, что стоит заменить критерий
на среднее по справедливым маркам (вырожденный путь = штраф), как оптимум
становится интерьерным и подтверждается полной симуляцией.

Чтобы получить осмысленную задачу и решать её формулами разделов 3–4:

1. **Марки.** Считать $\Phi$ по справедливым ценам ($P_{X2} = 1$,
   $P_{Y_v} = m_v$ или якорь), а не по ценам CE, которые задают сами агенты.
2. **Шейдинг арбитражёров.** Брать $\sigma^2$ в $g_X, g_Y$ из волатильности
   бирж (как у трансляторов), а не из собственного базиса на CE; это убирает
   положительную обратную связь и срыв.
3. **Ограничение на поток / инвентарь склада** (или штраф за дисперсию PnL):
   после (1)–(2) единственный ограничитель — риск склада, и оптимум — это
   компромисс $\max_\theta \big[T\pi(\theta) - \mu\,\operatorname{Var}\Phi(\theta)\big]$ либо
   $\max T\pi$ при $\operatorname{rms}(n_A)\le \bar n$; обе величины LG-модель даёт в
   замкнутой форме, а редуцированная симуляция уточняет на записанных путях
   (0.4 с на прогон против 60–80 с).

Практический рецепт после исправления постановки: (а) структура — из
раздела 3.3 (T1 склад, T2 немедленный хеджер, арбитражёры проводники, $h_m$
минимальные при $G/R<1$, $4ab\ll1$); (б) уровень — Нелдер–Мид по редуцированной
модели на 6 записанных путях с общими случайными числами, критерий — среднее
по справедливым маркам (медиана маскирует вырожденные пути); (в) контроль —
полная симуляция на тех же сидах с проверкой $P_{X2}(T)\approx 1$.

---

## 10. Почему оптимальные $h_m$ уходят к нулю: доказательство и интуиция

### 10.1 Утверждение

В регулярном режиме при фиксированных риск-коэффициентах ожидаемая прибыль
экосистемы **монотонно растёт по проводимости контура $1/R$** на всём
диапазоне устойчивости и не имеет интерьерного максимума по $h_m$; рост
останавливает только расходимость инвентаря склада (взрывы, клэмп), то есть
техника марок, а не экономика хеджа.

### 10.2 Доказательство в одномерной модели склада

Возьмём каноническую модель раздела 3.2: T2 хеджируется немедленно (его
инвентарь сидит на границе мёртвой зоны, шейдинг равен полуспреду и
перекладывается на кольцо), склад — T1 плюс арбитражёры с суммарным шейдингом
$G$, $\delta_t$ — AR(1) с параметром $\rho$ и дисперсией $\sigma^2$. Тогда

$$
V_t = \frac{\delta_t - G n_{t-1}}{R},\qquad
n_t = a\,n_{t-1} + \frac{\delta_t}{R},\qquad a = 1 - \frac{G}{R}.
$$

Прибыль за тик — объём хеджа T2 (равен $V_t$) на исполняемую премию
$\pi_t = r_2\delta_t - c_2\operatorname{sgn}V_t$, где премия **не зависит от $R$**.
Валовая часть:

$$
\mathbb{E}[n_{t-1}\delta_t] = \sum_{k\ge0} a^k\,\frac{\mathbb{E}[\delta_{t-1-k}\delta_t]}{R}
= \frac{\sigma^2\rho}{R(1-a\rho)},
\qquad
\boxed{\;\mathbb{E}[V_t\delta_t] = \frac{\sigma^2(1-\rho)}{R(1-\rho) + G\rho}\;}
$$

(подстановка $a = 1 - G/R$). Знаменатель убывает при уменьшении $R$, значит
$\mathbb{E}[V\delta]$ растёт по $1/R$ **при любом** $R$ из диапазона устойчивости
$G/R < 2$; при $G\to0$ это ровно $\sigma^2/R$ — линейно по потоку без
насыщения; при $G>0$ предел $R\to0$ равен $\sigma^2(1-\rho)/(G\rho)\propto 1/G$,
то есть и по $G$ планировщик хочет к нулю. Стоимость $c_2\mathbb{E}|V|$ растёт с тем
же множителем, поэтому чистая прибыль есть (объём) $\times$ (маржа), где маржа
$\mathbb{E}[\pi\mid\text{хедж}]>0$ по построению правила хеджа (T2 хеджируется лишь
когда $c_2(t) < g_2|u|$). Инвентарь склада:

$$
\mathbb{E}[n^2] = \frac{\sigma^2}{R^2}\,\frac{1+a\rho}{(1-a^2)(1-a\rho)}
\;\xrightarrow{\;a\to-1\;}\;\infty,\qquad
G = 0:\ \operatorname{Var}n_T \approx \frac{T\sigma^2(1+\rho)}{R^2(1-\rho)} .
$$

Итак, по $1/R$ прибыль растёт монотонно, а первым расходится инвентарь —
через переоценку по маркам с шумом 1% за тик он и даёт взрывы. $\blacksquare$

**Какие $h_m$ важны.** $R = \sum_a 1/\lambda_a$ — последовательная цепь: её
определяет самый осторожный агент. При нейтральных параметрах
$1/\lambda = (0.010,\,0.081,\,0.10,\,0.10)$ для (T1, T2, AX, AY): T1 даёт 3% $R$,
T2 и арбитражёры — по 30–35%. Выигрыш от уменьшения $h_{m,a}$ пропорционален
доле агента в $R$, поэтому GP тянет к нулю $h_{m2}, h_{mx}, h_{my}$, а $h_{m1}$
оставляет где угодно (в логе 0.03…7 при тех же медианах).

**Коллективно против индивидуально.** Доля агента в излишке исполнения
$\varphi_a\varepsilon V = \varepsilon^2\,x_a/(x_a+R_{-a})^2$, $x_a = 1/\lambda_a$,
максимальна при $x_a = R_{-a}$: агент, максимизирующий *свою* долю, поставил
бы сопротивление, равное сумме остальных (как олигополист, сдерживающий объём
ради цены). Сумма же $\sum_a\varphi_a\varepsilon V = \varepsilon^2/R$ растёт при $\lambda_a\to\infty$
для всех — оптимизируется сумма PnL, поэтому «все на полную».

### 10.3 Численная проверка (редуцированная модель, справедливые марки, 6 записанных путей)

Общий множитель $s$ для $h_{m2}, h_{mx}, h_{my}$ при остальных из регулярного
оптимума:

| $s$ | $1/R$ | средний PnL | поток за тик | rms $n_A$ | вырожденных | $G/R$ |
|---|---|---|---|---|---|---|
| 30 | 6 | +5.0 | 0.08 | 5 | 0/6 | 0.05 |
| 10 | 17 | +15.0 | 0.20 | 7 | 0/6 | 0.14 |
| 3 | 50 | +40.2 | 0.49 | 13 | 0/6 | 0.37 |
| 1 | 111 | +83.3 | 0.93 | 24 | 0/6 | 0.68 |
| 0.3 | 196 | +151.9 | 1.78 | 95 | 4/6 | 0.91 |
| 0.1 | 251 | +126.2 | 2.79 | 216 | 6/6 | 0.98 |
| 0.03 | 278 | −9.7 | 4.24 | 215 | 6/6 | 1.02 |

До границы вырождения прибыль линейна по $1/R$ (около $0.75$ X1 на единицу
$1/R$), после — клэмп и лотерея.

### 10.4 Экономическая интуиция

1. **Экосистема — маркет-мейкер на тонкой бирже с бесплатной справочной ценой
   с толстой.** Её маржа на единицу — исполняемая премия (сигнал 2.4% против
   полуспреда 1.8%, с отбором моментов узкого спреда ещё лучше), и эта маржа
   **не зависит от объёма**: контрагент — фоновые шумовые трейдеры с
   фиксированным профилем цен $N(a,\sigma_p)$, без информации и без реакции на
   агентов, книга восстанавливается сама, влияние хеджа на мид при этих объёмах
   пренебрежимо. Прибыль = маржа $\times$ объём, объём выбирают агенты через
   $\lambda = \kappa H/h_m$, значит объём хочется бесконечный. В реальном рынке
   маржа падает с объёмом (импакт, отбор информированных, конечная глубина);
   здесь до глубины дело не доходит — раньше срабатывают марки и клэмп.
2. **Критерий нейтрален к риску.** Сумма PnL не штрафует дисперсию, уровень
   цен — мартингал, фондирования нет: держать инвентарь в среднем ничего не
   стоит. Поэтому планировщик выключает неприятие риска там, где оно лишь
   режет объём ($h_{r1}\to0$: T1 — бесконечный склад; хедж T1 к тому же убыточен,
   его премия на толстой бирже отрицательна), и оставляет его только у T2, где
   мёртвая зона — это фильтр «хеджируйся, когда спред узкий».
3. **Контур — последовательная цепь, агенты — комплементы, а не конкуренты.**
   Самый осторожный тормозит всех; коллективный оптимум — полная проводимость у
   всех, хотя каждый в отдельности предпочёл бы быть узким местом и забирать
   бóльшую долю расхождения.
4. **Что на самом деле ограничивает.** Склад блуждает ($\operatorname{Var}n\propto T/R^2$),
   его марка шумит на 1% за тик — отсюда взрывы; шейдинг арбитражёров через
   дисперсию собственного базиса имеет положительную обратную связь — отсюда
   клэмп и отрыв марок. Оба ограничителя — свойства учёта, а не экономики; при
   справедливых марках и устойчивом шейдинге следующим ограничителем стала бы
   глубина тонкой биржи (хедж в 4 штуки сдвигает мид на 1.7% — порядок всей
   маржи).

---

## 11. Контрольный эксперимент: справедливые марки и постоянный риск арбитражёров

Полная симуляция без изменений кода агентов, две поправки только в *учёте*
(скрипт `fair_scan.py` в рабочих файлах сессии): PnL по справедливым маркам
($X2 = 1$, $Y_v$ = мид биржи $v$), банкротство по ним же; $\sigma^2$ в риск-коэффициенте
арбитражёров — константа из волатильности бирж после прогрева (как $\gamma$),
а не EWMA собственного базиса. Множитель $s$ для $h_{m2}, h_{mx}, h_{my}$ вокруг
регулярного оптимума, 6 сидов:

| $s$ | $1/R$ | средний PnL | std | банкротств | поток/тик | хедж T2, шт/тик | rms $n_A$ | $P_{X2}(T)$ | $\max|S_{X2}|$ |
|---|---|---|---|---|---|---|---|---|---|
| 3 | 50 | +27.0 | 4.5 | 0/6 | 0.45 | 0.0015 | 4 | 0.998 | 0.05 |
| 1 | 111 | +45.7 | 9.9 | 0/6 | 0.97 | 0.0038 | 13 | 0.930 | 0.12 |
| 0.3 | 196 | +86.1 | 74 | 0/6 | 2.2 | 0.026 | 148 | 0.516 | 0.78 |
| 0.1 | 251 | +170 | 128 | 4/6 | 3.2 | 0.056 | 361 | 0.368 | 1.03 |
| 0.03 | 278 | −75 | 176 | 6/6 | 3.8 | 0.078 | 566 | — | 1.03 |
| 0.01 | 290 | −389 | 143 | 6/6 | 1.2 | 0.023 | 1024 | — | 1.03 |

Что видно:

* оптимум становится **интерьерным** без единой новой издержки: по среднему
  с учётом банкротств — около $s = 0.3$, с поправкой на дисперсию — около $s = 1$
  (Шарп 4.6 против 1.2);
* ограничивает его **ёмкость склада арбитражёров**: они держат накопленный
  ток контура $n_A$ и не могут хеджироваться; их шейдинг $g_A n_A$ доходит до
  клэмпа при $n_A \approx 1/g_A = C/(\gamma\sigma^2) \approx 290$ X1 — это капитал,
  а не сбор; дальше марки CE отрываются, внутренние трансферы (T1 +940,
  T2 −466, AY −451) банкротят агентов по одному;
* глубина книги при этих объёмах ещё не ограничивает: хедж T2 — 0.004–0.08 шт
  за тик против 3 фоновых заявок по 1 шт за тик на тонкой бирже; спред биржи 2
  не меняется (3.44–3.69).
