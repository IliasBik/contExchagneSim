# Задача оптимизации агентов: математическая постановка

Документ к `optimize_agents.py`, `opt_simulation.py`, `param_heatmaps.py`.
Все формулы взяты из кода (`agent_formulas.py`, `agent_simulation.py`,
`coupled_market.py`, `pfx_exchange.py`); константы — из `SimConfig`
и настроек оптимизатора.

---

## 1. Постановка в одну строку

Вектор решений — шесть гиперпараметров агентов:

$$
\theta = (h_{m1},\, h_{r1},\, h_{m2},\, h_{r2},\, h_{mx},\, h_{my})
\in \Theta = [10^{-4},\, 300]^6 .
$$

Симуляция — стохастическое отображение $(\theta, \omega) \mapsto \Phi(\theta,\omega)$,
где $\omega$ — сид генератора (весь фоновый поток и фундаментальные шоки).
Задача:

$$
\theta^\star = \arg\max_{\theta \in \Theta} J(\theta), \qquad
J(\theta) = \operatorname{med}_{\omega} \Phi(\theta, \omega),
$$

$$
\Phi(\theta,\omega) =
\begin{cases}
\displaystyle \sum_{i \in I} E_i(T;\theta,\omega), & \text{прогон дошёл до } T,\\[6pt]
-B_{\mathrm{pen}}, & \exists\, t\le T:\ \max_i |E_i(t)| > B_{\mathrm{lim}} \quad(\text{«взрыв»}),
\end{cases}
$$

где $I = \{T1, T2, AX, AY\}$, $E_i(t)$ — PnL агента $i$ в единицах X1 (mark-to-market
по ценам CE), $T$ — длина прогона. Оценка $J$ на практике — выборочная медиана
по $K$ независимым сидам:

$$
\hat J_K(\theta) = \operatorname{med}\{\Phi(\theta,\omega_1),\dots,\Phi(\theta,\omega_K)\},
\qquad \omega_k \overset{iid}{\sim} \text{Uniform}.
$$

Оптимизатор (gp_minimize / skopt.Optimizer) минимизирует $-\hat J_K$ в
логарифмических координатах $u = \ln\theta$.

Разделы ниже раскрывают, что такое $E_i(T)$: это выход динамической системы,
параметры которой — гиперпараметры `SimConfig`.

---

## 2. Гиперпараметры симуляции (константы задачи)

### 2.1 Рынок

| Символ | Поле `SimConfig` | Значение | Смысл |
|---|---|---|---|
| $T$ | `total_steps` | 6000 | тиков в прогоне |
| $W$ | `warmup` | 200 | тиков прогрева до старта агентов |
| $\delta$ | `tick_size` | 0.01 | шаг ценовой сетки |
| $P_0$ | `initial_price` | 100 | стартовая цена, якорь при $t=0$ |
| $d$ | `depth_band` | 4 | полоса $\pm d$ вокруг мида для глубины |
| $\tau_a$ | `anchor_half_life` | 20 | полураспад EWMA якоря |
| $\sigma_F$ | `fundamental_vol` | $10^{-3}$ | std лог-шока фундаментальной цены за тик |
| $\rho_v$ | `venue*.arrival_rate` | 10 (биржа 1), 3 (биржа 2) | интенсивность фоновых заявок, Пуассон |
| $s$ | `venue*.order_size` | 1 | размер фоновой заявки |
| $\tau$ | `venue*.order_ttl` | 80 | время жизни заявки, тиков |
| $\sigma_p$ | `venue*.price_std` | 10 | std цены фоновой заявки от якоря |
| $\tau_v$ | `venue*.ewma_half_life` | 10 | полураспад EWMA-волатильности мида |

### 2.2 Агенты

| Символ | Поле `SimConfig` | Значение | Смысл |
|---|---|---|---|
| $c_0$ | `c0` | 1000 | стартовый виртуальный капитал, X1 |
| $\kappa_T$ | `kappa_t` | 1 | мощность семейства трансляторов, 1/тик |
| $\kappa_A$ | `kappa_a` | 0.01 | мощность арбитражёра от капитала, 1/тик |
| $q_{\max}$ | `q_max_fraction` | 0.005 | хедж-порог как доля капитала (калибровка $\gamma$) |
| $\phi$ | `capital_floor_frac` | 0.01 | пол капитала в риск-коэффициенте, доля $c_0$ |
| $\kappa_c$ | `shading_clamp` | 1 | ограничение $|g q|$ в экспоненте |
| $\tau_b$ | `arb_vol_half_life` | 20 | полураспад EWMA-волатильности базиса |
| $n_{\mathrm{comp}}$ | `hedge_competition` | None $\Rightarrow$ 1 | число трансляторов, делящих книгу |
| — | `arb_shading` | True | шейдинг котировки арбитражёров |
| — | `evolution_start` | $10^9$ | эволюция отключена |

### 2.3 Оптимизатор (`optimize_agents.py`)

| Символ | Константа | Значение |
|---|---|---|
| $\Theta$ | `H_M_BOUNDS`, `H_R_BOUNDS` | $[10^{-4}, 300]$ по каждой оси, лог-равномерный prior |
| $K$ | `N_SEEDS` | 5 |
| $B_{\mathrm{lim}}$ | `BLOWUP_LIMIT` | 1000 ($= c_0$) |
| $B_{\mathrm{pen}}$ | `BLOWUP_PENALTY` | 1000 |
| $N$ | `N_CALLS` | 100 вычислений $\hat J_K$ |
| $N_0$ | `N_INITIAL` | 20 случайных точек до включения GP |
| $b$ | `N_WORKERS` | 12 — размер батча ask/tell (constant liar) |

---

## 3. Обозначения

* Активы CE: $j \in \mathcal{A} = \{X1, Y1, X2, Y2\}$. Биржа $v \in \{1,2\}$ торгует
  актив $Y_v$ за кэш $X_v$. Единица счёта CE — X1, то есть $P_{X1} \equiv 1$.
* $S(t) \in \mathbb{R}^4$ — лог-цены CE, $P_j = e^{S_j}$.
* $Q_{i,j}(t)$ — позиция агента $i$ по активу $j$ (штуки) в балансах CE; при $t=0$ все нули.
* $E_i(t) = \sum_j Q_{i,j}(t)\, P_j(t)$ — PnL (mark-to-market в X1).
* $C_i(t) = c_0 + E_i(t)$ — капитал агента.
* $\mathcal{B}_v(t)$ — стакан биржи $v$: множество заявок (сторона, цена, объём, тик рождения).
* $m_v, s_v, D_v, \sigma_v^2$ — мид, спред, глубина у мида и EWMA-дисперсия
  лог-приростов мида биржи $v$.

Гиперпараметры агентов: у трансляторов $(h_{m,i}, h_{r,i})$, $i \in \{T1, T2\}$,
у арбитражёров только $h_{m,i}$, $i \in \{AX, AY\}$; $h_{r} \equiv 1$ для арбитражёров.

---

## 4. Динамика одного тика

Порядок внутри тика $t = 1,\dots,T$: (4.1) фоновый рынок $\to$ (4.3)–(4.4) заявки
агентов $\to$ (4.5) клиринг CE $\to$ (4.6) хедж $\to$ (4.7) PnL и проверка взрыва.

### 4.1 Фоновый рынок (две лимитные биржи)

Для каждой биржи $v$:

1. **Истечение.** Из $\mathcal{B}_v$ удаляются заявки с возрастом $\ge \tau$.

2. **Якорь** (общий для обеих бирж), $\alpha_a = 1 - 2^{-1/\tau_a}$:
   $$
   a(t) = \Big[(1-\alpha_a)\, a(t-1) + \alpha_a \frac{\sum_v D_v m_v}{\sum_v D_v}\Big]\cdot e^{\sigma_F \xi_t},
   \qquad \xi_t \sim \mathcal{N}(0,1).
   $$

3. **Фоновый поток.** $N_v \sim \mathrm{Pois}(\rho_v)$ заявок; у каждой сторона
   $\sim \mathrm{Bernoulli}(1/2)$, цена $p = \max\!\big(\delta\,\mathrm{round}(\tilde p/\delta),\, \delta\big)$,
   $\tilde p \sim \mathcal{N}(a(t), \sigma_p^2)$, объём $s$.

4. **Батч-аукцион.** Единая цена
   $$
   p^\star = \arg\max_p \min\big(\mathrm{Demand}_v(p), \mathrm{Supply}_v(p)\big)
   $$
   (при равенстве — ближайшая к последней цене); исполнение по приоритету цены,
   на маржинальном уровне — pro-rata. Неисполненное остаётся в $\mathcal{B}_v$.

5. **Метрики** остаточной книги: $b_v = \max$ бид, $a_v = \min$ аск,
   $s_v = a_v - b_v$, $m_v = (a_v+b_v)/2$ (при пустой стороне — последняя цена),
   $$
   D_v = \sum_{o \in \mathcal{B}_v:\ |p_o - m_v| \le d} \mathrm{size}_o, \qquad
   \sigma_v^2(t) = (1-\alpha_v)\,\sigma_v^2(t-1) + \alpha_v \ln^2\!\frac{m_v(t)}{m_v(t-1)},
   \quad \alpha_v = 1 - 2^{-1/\tau_v}.
   $$

Ни одна из этих величин не зависит от $\theta$ напрямую; агенты влияют на стакан
только через хеджи (4.6).

### 4.2 Калибровка $\gamma$ (один раз, после прогрева)

$$
\gamma = \frac{\bar c}{\bar\sigma^2\, q_{\max}}, \qquad
\bar c = \frac12 \sum_{v} \frac{\max(s_v, \delta)}{2\, m_v}, \qquad
\bar\sigma^2 = \frac12 \sum_v \sigma_v^2 ,
$$

все величины — на момент $t = 0$ (конец прогрева). $\gamma$ зависит от $\omega$.
Начальные цены CE: $P_{X1} = P_{X2} = 1$, $P_{Y_v} = m_v(0)$.

### 4.3 Заявки трансляторов

Транслятор $i \in \{T1, T2\}$ с домашней биржей $v$ наблюдает $m_v, s_v, D_v, \sigma_v^2$
после шага 4.1 и выставляет на CE заявку $(w_i, z_i, \lambda_i)$ со сроком жизни один клиринг.

Качество книги:
$$
H_v = \frac{D_v\, m_v\, \delta}{\max(s_v, \delta)} \quad [\text{в кэше } X_v];
\qquad H_v = 0 \ \text{(заявки нет), если сторона книги пуста или } D_v = 0.
$$

Мощность (при одном агенте на семейство доля $\mathrm{share}_i = \mathbf{1}[C_i > 0]$):
$$
\lambda_i = \frac{\kappa_T\, H_v\, \mathbf{1}[C_i(t-1) > 0]}{h_{m,i}}, \qquad
\tilde\lambda_i = \lambda_i\, P_{X_v}(t-1) \quad [\text{в X1}].
$$

Риск-коэффициент и котировка:
$$
g_i = \frac{h_{r,i}\, \gamma\, \sigma_v^2}{\max\big(C_i(t-1),\, \phi c_0\big)}, \qquad
z_i = m_v \exp\!\Big(-\operatorname{clip}\big(g_i\, Q_{i,Y_v}(t-1)\, m_v,\ -\kappa_c,\ \kappa_c\big)\Big).
$$

Портфель заявки: $w_i = e_{Y_v} - e_{X_v}$ (купить $Y_v$ за $X_v$), индекс
$\Pi = P_{Y_v}/P_{X_v}$ сравним с $m_v$.

### 4.4 Заявки арбитражёров

Арбитражёр $i \in \{AX, AY\}$ торгует пару $(L_i, L_i')$: $AX$ — $(X1, X2)$,
$AY$ — $(Y1, Y2)$; $w_i = e_{L_i} - e_{L_i'}$, индекс — базис $r_i = P_{L_i}/P_{L_i'}$.
Волатильность базиса он оценивает сам по ценам CE, $\alpha_b = 1 - 2^{-1/\tau_b}$:
$$
\sigma_i^2(t) = (1-\alpha_b)\,\sigma_i^2(t-1) + \alpha_b \ln^2\!\frac{r_i(t-1)}{r_i(t-2)}, \qquad \sigma_i^2(0)=0 .
$$

$$
\lambda_i = \frac{\kappa_A \max\big(C_i(t-1), 0\big)}{h_{m,i}}, \qquad
g_i = \frac{\gamma\, \sigma_i^2}{\max\big(C_i(t-1), \phi c_0\big)}, \qquad
z_i = \exp\!\Big(-\operatorname{clip}\big(g_i\, Q_{i,L_i}(t-1)\, P_{L_i}(t-1),\ -\kappa_c,\ \kappa_c\big)\Big).
$$

### 4.5 Клиринг CE

Пусть $\mathcal{O}(t)$ — множество активных заявок $a$ с $(w_a, z_a, \tilde\lambda_a)$.
Новые лог-цены — решение взвешенной задачи наименьших квадратов с калибровкой $S_{X1} = 0$:
$$
S(t) = \arg\min_{S:\ S_{X1}=0}\ \sum_{a \in \mathcal{O}(t)} \tilde\lambda_a \big(w_a \cdot S - \ln z_a\big)^2 ,
$$
где среди решений берётся ближайшее к $S(t-1)$: приращение $\Delta S = A^{+}(b - A\,S(t-1))$
с $A = \sum_a \tilde\lambda_a w_a w_a^\top$, $b = \sum_a \tilde\lambda_a \ln z_a\, w_a$
(направления, которых никто не котирует, сохраняют прошлую цену). Это условие
нулевого чистого потока стоимости по каждому активу: $\sum_a \tilde\lambda_a f_a w_a = 0$.

Филлы ($dt = 1$):
$$
f_a = \ln z_a - w_a \cdot S(t), \qquad V_a = \tilde\lambda_a f_a \quad[\text{X1}], \qquad
\Delta Q^{CE}_{a,j} = \frac{V_a\, w_{a,j}}{P_j(t)} .
$$

Ключевое свойство: $\sum_a V_a w_a = 0$, поэтому $\sum_i \Delta Q^{CE}_{i,j} = 0$ для
каждого $j$ — клиринг CE лишь перераспределяет активы между четырьмя агентами и
не создаёт стоимости в момент сделки ($\sum_j \Delta Q^{CE}_{a,j} P_j = V_a \sum_j w_{a,j} = 0$).

### 4.6 Хедж трансляторов об домашний стакан

После клиринга транслятор $i$ с позицией $q = Q_{i,Y_v}(t)$ (после филлов) и
капиталом $C_i$ (после филлов) пересчитывает $g_i$ по формуле 4.3 и определяет
сторону: $\mathrm{sell}$ при $q > 0$, $\mathrm{buy}$ при $q < 0$. Пусть
$(p_\ell, v_\ell)_{\ell \ge 1}$ — уровни встречной стороны остаточной книги
$\mathcal{B}_v(t)$ от лучшего вглубь, потеря уровня
$$
\ell_\ell = \max\!\Big(\frac{m_v - p_\ell}{m_v}, 0\Big)\ (\mathrm{sell}), \qquad
\ell_\ell = \max\!\Big(\frac{p_\ell - m_v}{m_v}, 0\Big)\ (\mathrm{buy}).
$$

**План** $h_i \ge 0$ (штуки) — маржинальный обход: с остатком стоимости $R \leftarrow |q|\, m_v$
для $\ell = 1, 2, \dots$
$$
q^{\mathrm{stop}}_\ell = \frac{\ell_\ell}{g_i}, \qquad
\text{если } R \le q^{\mathrm{stop}}_\ell:\ \text{стоп}; \qquad
\text{take}_\ell = \min\!\Big(\frac{R - q^{\mathrm{stop}}_\ell}{m_v},\ \frac{v_\ell}{n_{\mathrm{comp}}}\Big),
\quad h_i \mathrel{+}= \text{take}_\ell,\quad R \mathrel{-}= \text{take}_\ell\, m_v ,
$$
стоп, если уровень взят не целиком. Затем $h_i \leftarrow \min(h_i, |q|)$.
Экономически: позиция сокращается на уровне $\ell$, пока предельный риск
$g_i \cdot R$ превышает предельную потерю $\ell_\ell$; при одном бесконечно
глубоком уровне это даёт порог $|q^\star| = \ell_1 / g_i$.

**Исполнение.** Рыночный проход по книге объёмом $h_i$: исполнено
$\hat h_i \le h_i$ штук по средней цене $\bar p_i$ (уровни лучше цены остановки
гаснут целиком, маржинальный уровень уменьшается pro-rata; исполненные фоновые
заявки исчезают из $\mathcal{B}_v$, последняя цена биржи $\leftarrow \bar p_i$).
Результат зачисляется в балансы CE:
$$
\Delta Q^{H}_{i,Y_v} = \mp \hat h_i, \qquad \Delta Q^{H}_{i,X_v} = \pm \hat h_i\, \bar p_i
\qquad(\text{верхний знак — sell}).
$$

Это единственный канал, по которому активы входят в CE извне и по которому агенты
меняют стаканы лимитных бирж.

### 4.7 PnL, взрыв, остановка

$$
E_i(t) = \sum_{j} Q_{i,j}(t)\, P_j(t), \qquad
\text{прогон обрывается на первом } t \text{ с } \max_i |E_i(t)| > B_{\mathrm{lim}} .
$$

Эволюционный отбор отключён (`evolution_start` $= 10^9$); при одном агенте на тип
он и не мог бы никого убрать.

---

## 5. Что именно максимизируется: разложение суммарного PnL

Обозначим совокупную позицию экосистемы $\bar Q_j(t) = \sum_i Q_{i,j}(t)$.
Так как клиринг CE не меняет $\bar Q$ (раздел 4.5), совокупная позиция — это
ровно накопленные хедж-потоки: $\bar Q_j(t) = \sum_{s \le t} \sum_i \Delta Q^{H}_{i,j}(s)$.
Внутри тика цены CE меняются один раз, поэтому разложение точное:

$$
\Phi(\theta,\omega) = \sum_{i} E_i(T)
= \sum_{t=1}^{T} \Bigg[
\underbrace{\sum_{j} \bar Q_j(t-1)\,\big(P_j(t) - P_j(t-1)\big)}_{\text{переоценка совокупного инвентаря по ценам CE}}
\;+\;
\underbrace{\sum_{i \in \{T1,T2\}} \pm\, \hat h_i(t)\,\big(\bar p_i(t)\, P_{X_v}(t) - P_{Y_v}(t)\big)}_{\text{хедж: цена стакана против оценки CE}}
\Bigg].
$$

Следствия для смысла целевой функции:

* **Единственный контрагент экосистемы — фоновый поток лимитных бирж.**
  Сделки на CE — трансферы между $T1, T2, AX, AY$; сумма PnL растёт только за счёт
  того, что трансляторы продают/покупают у фона по ценам стакана, отличным от оценок CE.
* **Открытый инвентарь оценивается по ценам CE, которые задают сами агенты**
  (через $z_i$). Поэтому при вырожденных $\theta$ (например, $h_{r1} \to 0$: T1
  почти не шейдит и не хеджирует) первое слагаемое может расти без реализации,
  а второе слагаемое, $\bar Q$ и разброс по сидам — вместе с ним. Именно это
  ловит защита $B_{\mathrm{lim}}$.
* Индивидуальные $E_i$ — перераспределение $\Phi$ внутри четвёрки; в $J$ они не входят.

Экономический механизм: трансляторы тянут $P_{Y_v}/P_{X_v}$ к мидам $m_v$ своих бирж,
арбитражёры — $P_{X1}/P_{X2}$ и $P_{Y1}/P_{Y2}$ к единице. Так как $m_1 \ne m_2$, система
заявок несовместна, клиринг находит компромисс, и инвентарь оседает у трансляторов,
которые сбрасывают его в стаканы. Прибыль экосистемы — разница между тем, что
фон платит на биржах, и тем, во что CE оценивает переносимый инвентарь.

---

## 6. Эффективные переменные: как $\theta$ входит в динамику

Шесть гиперпараметров появляются в уравнениях только в шести произведениях
с константами системы:

$$
\Lambda_1 = \frac{\kappa_T}{h_{m1}},\quad
\Lambda_2 = \frac{\kappa_T}{h_{m2}},\quad
\Lambda_X = \frac{\kappa_A}{h_{mx}},\quad
\Lambda_Y = \frac{\kappa_A}{h_{my}},\quad
\Gamma_1 = \gamma\, h_{r1},\quad
\Gamma_2 = \gamma\, h_{r2} .
$$

$\Lambda$ — агрессивность (нотионал за тик на единицу лог-расхождения, на единицу
$H_v$ или капитала), $\Gamma$ — неприятие риска трансляторов (у арбитражёров оно
зафиксировано на $\gamma$). Константы $\kappa_T, \kappa_A, \gamma$ лишь задают точку
«нейтрального» значения $\theta = \mathbf{1}$; при этом $\gamma$ откалибрована по
состоянию рынка после прогрева и потому случайна.
Задача (1) эквивалентна оптимизации по $(\Lambda_1, \Lambda_2, \Lambda_X, \Lambda_Y, \Gamma_1, \Gamma_2)$
на соответствующих лог-прямоугольниках.

---

## 7. Популяционная форма целевой функции

Пусть $p(\theta) = \mathbb{P}_\omega[\text{взрыв}]$, а $G_\theta$ — функция распределения
$\sum_i E_i(T)$ при условии, что взрыва не было. Поскольку $-B_{\mathrm{pen}}$ лежит
ниже всех здоровых значений,
$$
J(\theta) =
\begin{cases}
G_\theta^{-1}\!\Big(\dfrac{1/2 - p(\theta)}{1 - p(\theta)}\Big), & p(\theta) < 1/2,\\[10pt]
-B_{\mathrm{pen}}, & p(\theta) \ge 1/2 .
\end{cases}
$$

То есть максимизируется квантиль здорового распределения PnL, уровень которого
снижается с вероятностью взрыва: при $p = 0$ это медиана, при $p \to 1/2$ — нижний
хвост, при $p \ge 1/2$ — штраф. Это неявное вероятностное ограничение
$p(\theta) < 1/2$, а не жёсткое $p(\theta) = 0$: точка с двумя взрывами из пяти
сидов остаётся допустимой (что и наблюдается в `opt_log.csv`).

---

## 8. Численная схема

**Байесовская оптимизация** (`optimize_agents.py`):

* координаты $u = \ln\theta \in [\ln 10^{-4}, \ln 300]^6$, стартовые $N_0 = 20$ точек
  равномерны в $u$;
* суррогат — гауссовский процесс для $-\hat J_K(u)$ с гауссовским шумом
  (`noise="gaussian"`), функция выбора — ожидаемое улучшение (EI);
* батчи по $b = 12$ точек через `ask(n_points=b, strategy="cl_min")`, счёт параллельно,
  затем `tell`; всего $N = 100$ вычислений $\hat J_K$, в каждом $K = 5$ свежих сидов
  (без общих случайных чисел между точками);
* ответ — $\arg\max_n \hat J_K(\theta_n)$ по всем посчитанным точкам.

**Тепловые карты** (`param_heatmaps.py`) — двумерные срезы $J$: при фиксированном
базовом $\bar\theta$ и паре координат $(k, l)$
$$
J_{k,l}(x, y) = \operatorname{med}_{\omega \in \{\omega_1,\omega_2,\omega_3\}}
\Phi\big(\bar\theta[\theta_k \leftarrow x, \theta_l \leftarrow y],\ \omega\big),
\qquad (x, y) \in \mathrm{geomspace}(10^{-4}, 10^2, 15)^2,
$$
с общими сидами для всех ячеек (common random numbers); медиана берётся только по
невзорвавшимся прогонам, ячейка помечается NaN, если взорвалось большинство.

---

## 9. Свойства задачи

* Чёрный ящик без градиента: внутри — дискретные аукционы, обход уровней стакана,
  клиппинг, индикаторы $\mathbf{1}[C_i > 0]$; $\Phi$ разрывна по $\theta$.
* Шум: при фиксированном $\theta$ разброс $\Phi$ по $\omega$ сопоставим с самим
  значением (в логе `std` в единицы при медиане в десятки для устойчивых точек и
  сотни для вырожденных).
* Невыпуклость и вырождения: суммарный PnL включает нереализованную переоценку по
  собственным ценам, поэтому у $J$ есть высокие, но неустойчивые пики на границе
  области взрывов.
* Масштабная симметрия: $h_{m,i}$ и $\kappa$, $h_{r,i}$ и $\gamma$ входят только
  произведениями (раздел 6); $\gamma$ зависит от сида.
